"use strict";

const main = (
	async	function main( ){
				$( "#field-deduction_code" )
				.chosen(
					(
						{
							"width": "300px"
						}
					)
				);
			}
);

(
	(
		async	function( ){
					await	main( );
				}
	)( )
);
