"use strict";

const main = (
	async	function main( ){
				$( "#field-deduction_code" ).chosen( )
			}
);

(
	await	(
				(
					async	function( ){
								await	main( );
							}
				)( )
			)
);
